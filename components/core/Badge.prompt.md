Small colored pill for labeling attractions, age ranges, or facility notes.

```jsx
<Badge color="blue">Ages 1-3 welcome</Badge>
```

Pick a color per category, not per instance (e.g. all age tags = blue, all facility notes = green) so pages stay scannable.
